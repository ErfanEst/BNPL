# === Build Artifacts ===
target/
*.jar
*.war
*.class

# Explicitly ignore your JAR
ETL.jar

# If you're outputting JARs anywhere else, catch them
**/*.jar

# === Docker ===
*.tar
*.img
*.docker
**/docker-compose.override.yml
**/docker-compose.yml
docker-compose.override.yml
docker-compose.yml

# Optional: exclude Docker image layers
**/.docker
**/docker/

# === Keep your Dockerfile but ignore build output ===
# (Only add the next line if you ever export Docker images to files)
*.tar.gz
